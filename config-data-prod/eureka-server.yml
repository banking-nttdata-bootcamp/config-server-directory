server:
  port: 8761

eureka:
  instance:
    hostname: https://bnttdata-eureka-server.azurewebsites.net
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      default-zone: http://${eureka.instance.hostname}/eureka/